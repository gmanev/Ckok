<?xml version="1.0" encoding="UTF-8"?>
<features>

    <feature name="ckok" version="${pom.version}">
    	<feature>cxf-tools</feature>
        <bundle>mvn:net.nbt.ckok/ckok-model/${pom.version}</bundle>
        <bundle>mvn:net.nbt.ckok/ckok-dao-jpa/${pom.version}</bundle>
        <bundle>mvn:net.nbt.ckok/ckok-server/${pom.version}</bundle>
    </feature>

    <feature name="ckok-ui-vaadin" version="${pom.version}">
        <feature>vaadin-bridge</feature>
        <bundle>mvn:net.nbt.ckok/ckok-model/${pom.version}</bundle>
        <bundle>mvn:net.nbt.ckok/ckok-proxy/${pom.version}</bundle>
        <bundle>mvn:net.nbt.ckok/ckok-ui-vaadin/${pom.version}</bundle>
    </feature>
 
</features>